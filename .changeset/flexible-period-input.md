---
"@octabits-io/nuxt-ui-kit": minor
---

Add `FlexiblePeriodInput` component (date window + stay length in nights, composing `DateRangeInput kind="travel"`) and `calculateNights` date helper (exclusive-end night count).
