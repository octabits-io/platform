node_modules/
dist/
*.tsbuildinfo
.turbo/

# Nuxt (apps/demo-web)
.nuxt/
.output/
.data/

# PGlite data dir (apps/demo-server, the zero-config default backend)
.pglite/
.DS_Store
bun.lock
CLAUDE.md
.claude/
.env*
# …but committed templates are the point of a template.
!.env.example

# local npm auth via direnv + passage
.envrc
.npmrc
